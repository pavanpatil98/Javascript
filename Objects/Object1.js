let response = {
    name:"Pavan Patil",
    company:"Avalara",
    department:"IT"
}
//keys are unique in object 
console.log(Object.keys(response));
console.log(Object.values(response));

//to fetch complete pair of key->value
console.log(Object.entries(response));